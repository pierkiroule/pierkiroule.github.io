const test=require("node:test");const assert=require("node:assert/strict");const physics=require("./bubble-physics.js"),feel=require("./game-feel.js");
test("une poussée accélère et le relâchement conserve une glisse aérienne",()=>{const s=physics.createState();for(let i=0;i<30;i++)physics.step(s,{forceX:.5,forceY:0},1/60,feel);const speed=s.vx;for(let i=0;i<30;i++)physics.step(s,{forceX:0,forceY:0},1/60,feel);assert.ok(speed>0&&s.vx>speed*.5&&s.vx<speed);});
test("la limite de vitesse est une résistance progressive sans clamp",()=>{const s=physics.createState();s.vx=feel.softMaxSpeed*2;physics.step(s,{forceX:1,forceY:0},1/60,feel);assert.ok(s.vx>feel.softMaxSpeed&&s.vx<feel.softMaxSpeed*2);});
test("POUF suit la trajectoire et reste doux",()=>{const s=physics.createState();s.vx=20;s.vy=10;physics.puff(s,feel.puffStrength);assert.ok(s.vx>20&&s.vy>10&&Math.hypot(s.vx,s.vy)<60);});
test("la membrane oscille puis retrouve presque son volume",()=>{const s=physics.createState();physics.collide(s,1,feel.membraneCollision);for(let i=0;i<120;i++)physics.step(s,{forceX:0,forceY:0},1/60,feel);assert.ok(Math.abs(s.scaleX-1)<.01&&Math.abs(s.scaleY-1)<.01);});
test("reste organique et stable pendant trente secondes de poussées et de glisse",()=>{const s=physics.createState();for(let frame=0;frame<1800;frame++){const phase=frame/60,active=frame%240<90;physics.step(s,{forceX:active?.28*Math.sin(phase*.7):0,forceY:active?.22*Math.sin(phase*1.3):0},1/60,feel);assert.ok(Number.isFinite(s.vx)&&Number.isFinite(s.vy));assert.ok(s.scaleX>.9&&s.scaleX<1.1&&s.scaleY>.9&&s.scaleY<1.1);}assert.ok(Math.hypot(s.vx,s.vy)<feel.softMaxSpeed*1.1);});
